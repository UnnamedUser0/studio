diff --git a/src/components/map/pizza-map.tsx b/src/components/map/pizza-map.tsx
index 0000000..1111111 100644
--- a/src/components/map/pizza-map.tsx
+++ b/src/components/map/pizza-map.tsx
@@ -89,141 +89,53 @@ export default function PizzaMap({
     toast({
       title: 'Obteniendo ubicación...',
-      description: 'Buscando tu posición actual con alta precisión.',
     });
 
-    let watchId: number | undefined;
-    let bestAccuracy = Infinity;
-    let updateCount = 0;
-    let hasTriedFallback = false;
-
-    const updateMap = (position: GeolocationPosition) => {
-      const { latitude, longitude, accuracy } = position.coords;
-      console.log(`Location update ${++updateCount}: ${latitude}, ${longitude} (Accuracy: ${accuracy}m)`);
-
-      if (accuracy < bestAccuracy) {
-        bestAccuracy = accuracy;
-        const latlng = new L.LatLng(latitude, longitude);
+    // Simple and fast like Google Maps
+    navigator.geolocation.getCurrentPosition(
+      (position) => {
+        const { latitude, longitude, accuracy } = position.coords;
+        console.log(`Location: ${latitude}, ${longitude} (Accuracy: ${accuracy}m)`);
+
+        const latlng = new L.LatLng(latitude, longitude);
         setUserLocation({ lat: latitude, lng: longitude });
 
         if (myLocationMarkerRef.current) {
           myLocationMarkerRef.current.setLatLng(latlng);
         } else {
           myLocationMarkerRef.current = L.marker(latlng, { icon: myLocationIcon }).addTo(map);
         }
 
         map.flyTo(latlng, 16);
         onLocateUser({ lat: latitude, lng: longitude });
-        
-        // Show accuracy feedback
+
         toast({
-          title: 'Ubicación actualizada',
+          title: 'Ubicación encontrada',
           description: `Precisión: ${accuracy.toFixed(0)} metros`,
         });
-      }
-    };
-
-    const tryLowAccuracy = () => {
-      if (hasTriedFallback) return;
-      hasTriedFallback = true;
-
-      toast({
-        title: 'Intentando con menor precisión...',
-        description: 'Búsqueda rápida activada.',
-      });
-
-      // Try with lower accuracy (faster but less precise)
-      navigator.geolocation.getCurrentPosition(
-        (position) => {
-          updateMap(position);
-          toast({
-            title: 'Ubicación encontrada',
-            description: `Precisión aproximada: ${position.coords.accuracy.toFixed(0)} metros`,
-          });
-        },
-        (error) => {
-          console.error("Low accuracy geolocation also failed:", error.code, error.message);
-          toast({
-            variant: 'destructive',
-            title: 'No se pudo obtener ubicación',
-            description: 'Verifica que los servicios de ubicación estén habilitados y permitidos para este sitio.',
-          });
-        },
-        {
-          enableHighAccuracy: false,
-          timeout: 10000,
-          maximumAge: 60000 // Accept cached position up to 1 minute old
-        }
-      );
-    };
-
-    const onSuccess = (position: GeolocationPosition) => {
-      updateMap(position);
-
-      // Stop watching if we get very high accuracy (under 15m instead of 20m)
-      if (position.coords.accuracy < 15 && watchId !== undefined) {
-        navigator.geolocation.clearWatch(watchId);
-        watchId = undefined;
-        toast({
-          title: 'Ubicación precisa encontrada',
-          description: `Precisión: ${position.coords.accuracy.toFixed(0)} metros.`,
-        });
-      }
-    };
-
-    const onError = (error: GeolocationPositionError) => {
-      console.error("Geolocation error:", error.code, error.message);
-
-      if (watchId !== undefined) {
-        navigator.geolocation.clearWatch(watchId);
-        watchId = undefined;
-      }
-
-      // If timeout and haven't tried fallback, try with lower accuracy
-      if (error.code === error.TIMEOUT && !hasTriedFallback) {
-        tryLowAccuracy();
-        return;
-      }
+      },
+      (error) => {
+        console.error("Geolocation error:", error.code, error.message);
 
-      let errorMessage = 'No se pudo obtener tu ubicación.';
-      switch (error.code) {
-        case error.PERMISSION_DENIED:
-          errorMessage = 'Permiso denegado. Por favor habilita la ubicación en tu navegador.';
-          break;
-        case error.POSITION_UNAVAILABLE:
-          errorMessage = 'Ubicación no disponible. Verifica tu conexión y servicios de ubicación.';
-          break;
-        case error.TIMEOUT:
-          errorMessage = 'Tiempo agotado. Intenta moverte a un área más abierta.';
-          break;
-      }
+        let errorMessage = 'No se pudo obtener tu ubicación.';
+        switch (error.code) {
+          case error.PERMISSION_DENIED:
+            errorMessage = 'Permiso denegado. Por favor habilita la ubicación en tu navegador.';
+            break;
+          case error.POSITION_UNAVAILABLE:
+            errorMessage = 'Ubicación no disponible. Verifica tu conexión GPS.';
+            break;
+          case error.TIMEOUT:
+            errorMessage = 'Tiempo de espera agotado. Intenta de nuevo.';
+            break;
+        }
 
-      toast({
-        variant: 'destructive',
-        title: 'Error de ubicación',
-        description: errorMessage,
-      });
-    };
-
-    // Start with high accuracy
-    watchId = navigator.geolocation.watchPosition(onSuccess, onError, {
-      enableHighAccuracy: true,
-      timeout: 15000, // 15 seconds for first attempt
-      maximumAge: 0 // Do not accept cached positions
-    });
-
-    // Safety timeout - try low accuracy if no position found
-    setTimeout(() => {
-      if (watchId !== undefined) {
-        navigator.geolocation.clearWatch(watchId);
-        watchId = undefined;
-        if (bestAccuracy === Infinity) {
-          // Never got any position, trigger fallback
-          tryLowAccuracy();
-        } else {
-          toast({
-            title: 'Ubicación obtenida',
-            description: `Mejor precisión alcanzada: ${bestAccuracy.toFixed(0)} metros`,
-          });
+        toast({
+          variant: 'destructive',
+          title: 'Error de ubicación',
+          description: errorMessage,
+        });
+      },
+      {
+        enableHighAccuracy: true,
+        timeout: 10000,
+        maximumAge: 30000 // Accept recent cached position (like Google Maps)
         }
+    );
-    }, 16000);
   };
 
   const drawRoute = async (destination: { lat: number, lng: number }) => {
